from enum import auto
from enum import IntEnum


class Biome(IntEnum):
    DESERT = auto()
    FLOWERED = auto()
    GRASS = auto()
    TUNDRA = auto()
    OCEAN = auto()
    SAVANNA = auto()
    SNOW = auto()

